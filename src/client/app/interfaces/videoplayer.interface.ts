export interface IVideoJSConfig {
    id: string; // The id for the component, has to be unique for each player
    url: string;
    type: string;
    width: number;
    height: number;
    title:string;
}